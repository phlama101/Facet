// Minimum fraction of questions correct required to pass any quiz or capstone.
// Applied client-side (blocks completion) and server-side (rejects the API call).
export const QUIZ_PASSING_SCORE = 0.6
